# PostgreSQL Schema Guide

## AI Product Manager Copilot

---

# Purpose

This document defines the PostgreSQL table creation order for importing datasets generated by the Enterprise Synthetic Data Generation Engine (ESDGE).

---

# Database Creation Order

1. organizations
2. workspaces
3. users
4. projects
5. feedback
6. features
7. tickets
8. sprints
9. analytics_metrics
10. rag_chunks

---

# Primary Keys

organizations
- organization_id

workspaces
- workspace_id

users
- user_id

projects
- project_id

feedback
- feedback_id

features
- feature_id

tickets
- ticket_id

sprints
- sprint_id

analytics_metrics
- metric_id

rag_chunks
- chunk_id

---

# Foreign Key Relationships

workspaces.organization_id
    REFERENCES organizations.organization_id

users.workspace_id
    REFERENCES workspaces.workspace_id

projects.workspace_id
    REFERENCES workspaces.workspace_id

feedback.project_id
    REFERENCES projects.project_id

feedback.user_id
    REFERENCES users.user_id

features.project_id
    REFERENCES projects.project_id

tickets.project_id
    REFERENCES projects.project_id

tickets.user_id
    REFERENCES users.user_id

sprints.project_id
    REFERENCES projects.project_id

analytics_metrics.project_id
    REFERENCES projects.project_id

rag_chunks.feedback_id
    REFERENCES feedback.feedback_id

---

# Recommended Import Order

organizations.csv

↓

workspaces.csv

↓

users.csv

↓

projects.csv

↓

feedback.csv

↓

features.csv

↓

tickets.csv

↓

sprints.csv

↓

analytics_metrics.csv

↓

rag_chunks.csv

---

# Notes

- Preserve all generated IDs.
- Import parent tables before child tables.
- Maintain foreign key integrity.
- Do not regenerate IDs inside PostgreSQL.